{
  "name": "eco-champions",
  "version": "0.1.0",
  "private": true,
  "description": "Ecology Champions - collaborative sustainability tracking platform",
  "workspaces": [
    "api",
    "frontend"
  ],
  "scripts": {
    "dev": "concurrently -n api,web -c blue,green \"npm:dev:api\" \"npm:dev:web\"",
    "dev:api": "npm -w api run dev",
    "dev:web": "npm -w frontend run dev",
    "build": "npm -w api run build && npm -w frontend run build",
    "typecheck": "npm -w api run typecheck && npm -w frontend run typecheck"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  },
  "engines": {
    "node": ">=18"
  }
}
